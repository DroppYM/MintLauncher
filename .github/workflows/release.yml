name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "MintLauncher-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/MintLauncher-source MintLauncher-${{ env.VERSION }}
          mv MintLauncher-Linux-Qt6-Portable*/MintLauncher-portable.tar.gz MintLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv MintLauncher-Linux-aarch64-Qt6-Portable*/MintLauncher-portable.tar.gz MintLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv MintLauncher-*.AppImage/MintLauncher-*-x86_64.AppImage MintLauncher-Linux-x86_64.AppImage
          mv MintLauncher-*.AppImage.zsync/MintLauncher-*-x86_64.AppImage.zsync MintLauncher-Linux-x86_64.AppImage.zsync
          mv MintLauncher-*.AppImage/MintLauncher-*-aarch64.AppImage MintLauncher-Linux-aarch64.AppImage
          mv MintLauncher-*.AppImage.zsync/MintLauncher-*-aarch64.AppImage.zsync MintLauncher-Linux-aarch64.AppImage.zsync
          mv MintLauncher-macOS*/MintLauncher.zip MintLauncher-macOS-${{ env.VERSION }}.zip
          mv MintLauncher-macOS*/MintLauncher.dmg MintLauncher-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf MintLauncher-${{ env.VERSION }}.tar.gz MintLauncher-${{ env.VERSION }}

          for d in MintLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="MintLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv MintLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in MintLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="MintLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv MintLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in MintLauncher-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="MintLauncher-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv MintLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Mint Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            MintLauncher-Linux-x86_64.AppImage
            MintLauncher-Linux-x86_64.AppImage.zsync
            MintLauncher-Linux-aarch64.AppImage
            MintLauncher-Linux-aarch64.AppImage.zsync
            MintLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            MintLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            MintLauncher-Windows-MinGW-w64-${{ env.VERSION }}.zip
            MintLauncher-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            MintLauncher-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            MintLauncher-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            MintLauncher-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            MintLauncher-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            MintLauncher-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            MintLauncher-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            MintLauncher-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            MintLauncher-Windows-MSVC-${{ env.VERSION }}.zip
            MintLauncher-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            MintLauncher-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            MintLauncher-macOS-${{ env.VERSION }}.zip
            MintLauncher-macOS-${{ env.VERSION }}.dmg
            MintLauncher-${{ env.VERSION }}.tar.gz
